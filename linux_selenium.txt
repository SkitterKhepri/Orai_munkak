

from selenium import webdriver

d = webdriver.Chrome()

print(d)

d.__dict__

/* for(kulcs, ertek) in d[]
					ez nemtom mi a fasz akart lenni pontosan
	print(kulcs)			AHAHAHa még mindig megy egy kicsit, hmm, nem is rossz, lehetne gyorsabb, de 					most így tök oké. Snowdrop is the best
					
*/

d.get("http://python.org")

print(d.title)				



python3								full működő dolog
from selenium import webdriver
from selenium.webdriver.chrome.options import Options
op = Options()
op.add_argument("--headless")					fasz tudja mi akar lenni, talán gui nélkül
d.webdriver.Chrome(options = op)
